# Concord 工作台 UI 设计

本文定义工作台的视觉层级与交互方向，供后续 UI 修改和验收使用。状态：共享布局与本轮页面整理已实现，后续页面继续沿用这些规则。产品行为仍由 [Web 工作台契约](docs/feature/web-workbench/README.md) 和 [Use Case](docs/feature/web-workbench/use-case/use-web-workbench.md) 拥有；本文不重复维护功能或状态注册表。

适用约束：[事实来源](docs/constitution.md#c-001)、[证据表达](docs/constitution.md#c-004)、[自身验收](docs/constitution.md#c-006)、[协作边界](docs/constitution.md#c-007)。本设计只调整展示与入口组织，不改变持久数据、权限、状态转换或资源所有权。

## 目标与当前问题

让用户先看到正在维护的对象、内容和下一步操作。导航、分组、记录、辅助信息应有稳定且可辨认的层级。

用户截图暴露出三个连续问题：

- 黄色眉题（如 `Executable declarations`、`Feature 内部`）重复解释页面，增加阅读负担。
- 大框包小框、亮白描边和大面积空卡片抢走视觉注意力；取消全部边界后，长列表又缺少可辨认的条目单位。
- Feature 的生命周期页只重复类型、ID、创建时间，并显示“此类型没有额外状态转换”，却占用完整页签和两个大卡片。

因此，页面使用标题与留白建立层级，重复条目使用轻分隔线，独立操作区域才使用面板。布局密度由信息量决定，不为填满屏幕拉高空容器。

## 页面层级与操作归属

| 层级 | 表达方式 | 内容与操作 |
| --- | --- | --- |
| 工作区导航 | 稳定侧栏、当前项底色 | Feature、反馈、Git、设置等入口 |
| 对象导航 | 名称列表、筛选、选中态 | 新建 Feature 等集合操作放在列表附近 |
| 当前对象 | 明确的名称上下文、页签 | 查看当前对象 Git 变更等对象操作 |
| 页签内容 | 紧凑工具栏、分组标题 | 添加关联、筛选、运行等当前任务操作 |
| 内容条目 | 主名称、次级位置、轻分隔线 | 打开源码、查看记录 |
| 辅助细节 | 可展开详情或菜单 | ID、完整引用、复制等低频操作 |

同一操作在同一视图只保留一个主要入口。已经有明确页签上下文时，不再添加巨大的同义标题和解释性眉题。必要的小标题可以用于对齐工具栏。

黄色用于告警、待处理或需要注意的状态，不作为页面分类装饰。分支名称等真实上下文保留为普通次级文本或状态标签。错误与保存冲突必须继续清晰可见。

## 表面、边框与间距

页签内容不额外增加顶部外边距。共享 `PanelHeader` 保持统一的最小高度，即使没有操作按钮也不改变标题基线。页签空状态使用 `PanelEmpty` 的左对齐文字和轻分隔线，不使用大虚线框、搜索失败图标或固定高度占位。页签内新建与添加操作统一使用 outline 按钮。

| 元素 | 视觉规则 |
| --- | --- |
| 页面背景 | 使用 `--background`，不加包围整个内容区的边框 |
| 功能分组 | 标题与统计在同一行，组间留白 24–32px；必要时使用一条分隔线 |
| 重复记录 | 上下内边距 12–16px，以 1px 低对比度横线分隔；不逐条套圆角卡片 |
| 独立表单或摘要面板 | 轻微底色差异，最多一层容器，内边距 16–24px，无常驻重阴影 |
| 输入框、按钮 | 保留可识别控件边界，和纯内容分隔线分别调整 |
| 抽屉、弹窗 | 使用独立表面与遮罩，表达临时上下文 |
| 键盘焦点 | 清晰的主题色焦点环；不得因减弱装饰边框而减弱焦点提示 |

深色主题的内容分隔线以现有 `--border: #344144` 为起点；浅色以 `#d8d8cf` 为起点。描边不能使用正文色或近白色。最终以浏览器计算样式和明暗主题截图验收，不只检查变量声明。

`Card` 复用 `Surface`，明确绑定主题边框色，浏览器检查确保描边不继承正文色。普通内容分组使用 `ContentSection`，真正需要边界的控件仍保留。

标题建议：页面 24–28px、页签工具栏 18px、分组与条目 14–16px。分组通过间距和位置区别于条目，不靠不断放大字号。次级信息保持可读，长路径允许换行；源码本身保持横向滚动。

## 实现页

直接按功能名称分组，不加 `Feature 公共实现` 或 `Use Case ·` 前缀。代码关联 Feature 并不能证明它属于公共实现。Use Case 名称可进入详情。

```text
实现                                      ＋ 添加关联

Agents 与 Adapters                         18 处 · 13 个文件
────────────────────────────────────────────────────────
  aiSdkAgent                                  类／函数
  packages/…/agents/ai-sdk.ts                   L490–572
  ▸ 声明详情
────────────────────────────────────────────────────────
  bindAdapterEvalContext                      类／函数
  packages/…/adapter.ts                        L380–390
  ▸ 声明详情
────────────────────────────────────────────────────────
```

横线使用弱边框色，不使用示意图字符的高对比度。每条记录具有完整点击目标：源码位置打开抽屉，详情独立展开；不能把整行点击和内部控件混成一个交互。悬停提供轻底色反馈。

默认展示符号名称、范围类型、文件路径和行号。声明 ID、复制操作、完整契约引用放在默认折叠的详情中。示意路径省略仅为排版，实际页面应允许读到完整路径。

顶部统一添加关联，在弹窗中选择目标功能。多对多关系允许代码出现在多个所属分组，各组统计只描述该组，不据此声称全页唯一代码总数。无关联的分组使用一行空状态；全页没有关联时提供接入说明，避免重复的大空卡片。

## 生命周期与元数据

没有状态转换或生命周期历史的类型，不显示生命周期页签。Feature、Use Case 等基本身份信息归入元数据；截图中的“此类型没有额外状态转换”占位卡应移除。

Roadmap、Design、Memory、Issue 等确有生命周期能力的对象保留入口。当前状态暂时没有动作时，仍可展示有效状态与历史，不因按钮为空就隐藏整个页签。

生命周期内容采用自然高度的纵向结构：顶部紧凑状态摘要，其次可用动作及理由表单，最后历史列表。不要强制左右等高卡片，也不留一块空白操作面板。危险动作单独分区，并保留原有确认、证据与授权要求。

旧 `?tab=actions` 链接在不支持生命周期的对象上回落到元数据；切换和回落遵守原有草稿保存与冲突保护。具体入口契约见所属 Use Case。

## 跨页面一致性

Use Cases、术语、实现、测试共用紧凑的 `PanelHeader` 与 `RecordList`。Use Case 行保留名称、ID 和详情导航；术语行展示首选名、英文名与定义。测试页直接展示完整关联列表，不再设置内部“测试声明／运行任务”页签、搜索框和计数工具栏；可执行条目的运行、取消和最近任务状态保留在条目内。

共享实现位于 `web/components/content-layout.tsx`：`PanelHeader` 统一页签标题与操作；`ContentSection` 管理分组标题及统计；`RecordList`、`RecordItem`、`RecordDetails` 统一条目分隔、密度与折叠细节；`Surface` 管理面板底色、圆角和主题边框。现有 `Card` 复用 `Surface`，保留原有组合 API。实现与测试列表已接入这些组件，其他页面逐步迁移；业务组件仍拥有字段、状态与操作权限。

测试页沿用实现页的紧凑工具栏与记录分隔方式，但保留运行状态、执行按钮和失败信息。命令收据不能被设计成原生测试覆盖证明。

元数据使用自然高度的字段分区；反馈与证据列表使用同一套分组、条目和辅助细节层级。健康诊断、来源读取失败和配置错误属于有效信息，不能作为“多余说明”统一删除。

手机端工具栏允许换行，计数可以移到标题下方，长路径不撑宽页面。详情展开后应保持所属条目边界。深浅主题采用相同的信息层级，不各自形成一套布局。

## URL 与导航状态

URL 是导航状态的唯一来源。文档 `tab`、正文 `file`、Use Case 路径、源码 `source/sourceLine/sourceEndLine`、设置 `tab`、Git `tab/path/area/line/view` 和结果 `resultView` 直接驱动组件；禁止用 `useState` 或 ref 复制当前导航值，再通过 effect 双向同步。Git 两类文件的上次选择保存在 URL 的 `docsPath/docsArea/testsPath/testsArea`。缺省与无效选择按页面能力回落，外部路径仍由服务端验证。

用户切换产生历史项；浏览器前进、后退和深链接使用同一条渲染路径。drawer 关闭只在当前运行实例能证明紧邻父项时后退，否则替换到明确父 URL；刷新后不信任旧 history state。源码关闭保留所属文档的 tab/file，Use Case 关闭回所属 Feature 的 Use Cases。历史 provenance 仅证明关闭去向，不保存可见选择。

编辑草稿、请求状态和临时表单继续由组件拥有。全局路由 blocker 统一处理离开前保存，不再维护第二个 pending destination。只读内容不注册自动保存；唯一活动草稿 owner 负责 flush、dirty 与 discard，旧 owner 清理不能影响新 owner。设置的表单与 JSON 在离开前保存当前表示；失败保留原 URL 和草稿，明确丢弃先恢复 baseline 并取消延迟保存。导航等待期间继续编辑必须保存最新 revision，过期导航回调不得跳转。

滚动缓存只属于展示层，按历史项记录桌面、移动容器与 drawer 位置；等待异步内容后恢复，用户主动操作停止补偿。刷新恢复导航位置，不承诺像素位置。结果页的视图可深链接，但瞬时命令返回值不会为恢复页面而自动重跑命令。

该导航方案经独立只读 Astra 设计挑战 PASS；交付需以浏览器验证保存冲突、非法 JSON、连续导航、嵌套关闭、刷新和前进后退。依据宪法 c-001、c-003、c-005、c-006、c-007。

## 实施与验收记录

1. 清除装饰眉题，保留有意义的状态信息；为实现记录恢复轻分隔线。
2. 检查共享 Card 和边框 token，消除近白描边，逐页确认必要面板与冗余外框。
3. 按类型能力调整生命周期入口，把身份信息集中到元数据，并处理旧链接与草稿保护。
4. 将测试、反馈、证据等页面对齐到同一视觉规则，避免仅修复单张截图。

每一步独立可回滚。本轮已接入共享标题、记录列表、空状态和字段分组；反馈来源进入独立页签，集合操作移入导航，生命周期按类型能力显示。运行状态、诊断、真实筛选和 Git 审阅控件仍按业务需要保留。

验收至少覆盖：无数据、单条与 18 条以上记录；1280px 和宽屏桌面、390px 手机；明暗主题；键盘焦点、详情展开、源码定位和关闭返回。长列表应能一眼辨认条目边界，又没有连续的亮白矩形框。没有实际内容的页签和空操作面板不占空间。

行为检查覆盖添加关联的目标选择、多对多分组、未保存内容保护和既有生命周期操作。视觉检查需要实际浏览器截图与计算样式；`pnpm check` 验证构建、类型及打包消费者行为，不能替代视觉验收。
