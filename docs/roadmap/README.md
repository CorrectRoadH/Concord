# Roadmap

Roadmap 保存已经定稿、尚待采用为当前 Feature 的方向。`roadmap adopt` 复制完整 Markdown package、迁移当前 promotion 并保留 adopted 历史；采用后当前语义由新 Feature 拥有。

使用 `concord roadmap create --help` 创建，使用 `concord roadmap list/show` 查询。本仓库当前没有待采用 Roadmap。
