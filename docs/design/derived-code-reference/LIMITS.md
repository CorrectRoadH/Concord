# 实现声明自动派生查询引用

## L1: 源码是唯一 owner

源码继续是实现关系的唯一 owner；不增加身份登记表、数据库事实、sidecar 或自动写回 UUID。来源：原设计约束。判定依据是候选不引入第二身份来源。

## L2: 不改变相邻协议或证据含义

本轮不改变测试声明身份、Memory/evidence、配置和写入恢复协议。实现关联不证明完成或覆盖率。普通扫描不得改写源码。来源：原设计约束。判定依据是候选是否保持这些边界。

## L3: 只实现当前 Concord 协议

项目尚未发布，只实现当前协议，不保留旧命令、兼容层或迁移机制。只修改 Concord 自身及隔离测试消费者，不修改外部项目。来源：原设计约束。判定依据是候选是否避免 legacy 承诺和外部修改。

### 候选

[自动派生](plans/derived/README.md)：范围与关联保留，工具从当前源码计算查询引用。

[显式 ID](plans/explicit/README.md)：维护者或工具在源码保存额外身份。
