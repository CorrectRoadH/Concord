# 验收场景

- Feature 和 Use Case 路径都能建立关系，错误目标类型或不存在的目标形成诊断。
- 多个不同测试可指向同一契约；同一文件内无法唯一绑定的同名声明形成诊断。
- 改名或移动文件后重新派生执行引用。
- 关系注释更新不改变代码投影；名称、断言、helper 或文件映射变化会改变投影，契约路径和内容由 binding 单独约束。
- 内置和打包 CLI、Web 展示使用同一现行关系，原生证据不由静态扫描代替。
