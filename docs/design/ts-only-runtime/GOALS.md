# 目标

## G1: 普通入口只接受静态 TS 配置

普通入口只接受静态 TS 配置；旧格式以具名错误引导显式离线迁移。来源：原设计目标和用户无 legacy 要求。判定依据是入口对 concord.config.ts、旧格式和双配置的分类行为。

## G2: 旧现场拒绝不破坏恢复现场

错误诊断保护中断现场，当前事务保持精确恢复。来源：原设计目标。判定依据是旧 journal、锁、配置、目标和临时文件均只读拒绝，不被普通运行时改写。
