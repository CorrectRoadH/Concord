# 限制

## L1: 不提供旧版本运行时兼容

不增加旧版本运行时兼容、不增加迁移 CLI、不翻译旧证据。来源：原设计约束。判定依据是旧格式由具名迁移诊断拒绝，旧证据重新取证。

## L2: 保留协调文件与领域语义

保留共享 lease、两套 journal 障碍以及当前领域数据语义。来源：原设计约束。判定依据是拒绝旧现场不清理锁/journal，现有数据语义不变。

## L3: 离线脚本独立拥有历史数据迁移

离线脚本独立拥有历史数据迁移。来源：原设计约束。判定依据是普通 runtime 不引入 registry 或 CLI fallback，迁移不被运行时冒充。
