# Limits

## L1: Safe publication

新增写入口仅授权固定规则路径，具名操作复用配置绑定、CAS、journal 与恢复，拒绝任意文件写入。

## L2: Preserve sources

保持概念 Markdown 的现有真源和身份，禁止静默迁移、复制登记表与旧格式运行时兼容。

## L3: Honest checks

静态规则与命中不表示实现或原生测试通过，不运行产品 runner，不自动重写全仓正文。
