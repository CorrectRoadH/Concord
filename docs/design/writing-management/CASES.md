# Cases

- 同一 ASCII 表达不同大小写重复登记，保存前指出冲突。
- 两个浏览器编辑同一文件，后保存者保留草稿并收到前像冲突。
- 概念表首选词变更，下一次查看/检查派生新规则，无需同步 JSON。
- 规则文件缺失、格式错误或 symlink：明确错误，已有文件不覆盖。
- prepared/committed 事务中断、配置变化和未知外部编辑：按恢复契约核验。
- 成功扫描后正文或规则变化，旧报告失效，不把旧零命中继续显示为当前状态。
