# Engineering Memory

Memory 保存 Problem、Decision 与可复用 Insight。生命周期、epoch、resolution、supersession 和 promotion history 必须通过 Concord 具名命令维护；不要手工重写 metadata。本仓库当前没有 Memory owner。

使用 `concord memory add --help` 创建，使用 `concord memory list/show/search` 查询。
